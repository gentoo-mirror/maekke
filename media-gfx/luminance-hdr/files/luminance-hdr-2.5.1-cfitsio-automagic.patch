diff -ru luminance-hdr-2.5.1.bak/CMakeLists.txt luminance-hdr-2.5.1/CMakeLists.txt
--- luminance-hdr-2.5.1.bak/CMakeLists.txt	2017-05-28 12:16:28.053899513 +0200
+++ luminance-hdr-2.5.1/CMakeLists.txt	2017-05-28 12:30:18.934939132 +0200
@@ -136,13 +136,16 @@
 FIND_PACKAGE(JPEG REQUIRED)
 INCLUDE_DIRECTORIES(${JPEGLIB_INCLUDE_DIR})
 
-FIND_PACKAGE(CFITSIO)
-IF(CFITSIO_FOUND)
-    INCLUDE_DIRECTORIES(${CFITSIO_INCLUDE_DIR})
-    SET(LIBS ${LIBS} ${CFITSIO_LIBRARIES})
-    ADD_DEFINITIONS(-DHAVE_CFITSIO)
-ELSE()
-    MESSAGE(WARNING "cfitsio not found! Building without FITS support!")
+OPTION(USE_FITS "Enable FITS support." ON)
+IF(USE_FITS)
+	FIND_PACKAGE(CFITSIO REQUIRED)
+	IF(CFITSIO_FOUND)
+	    INCLUDE_DIRECTORIES(${CFITSIO_INCLUDE_DIR})
+	    SET(LIBS ${LIBS} ${CFITSIO_LIBRARIES})
+	    ADD_DEFINITIONS(-DHAVE_CFITSIO)
+	ELSE()
+		MESSAGE(FATAL_ERROR "cfitsio not found! Building without FITS support!")
+	ENDIF()
 ENDIF()
 
 SET(LIBS ${LIBS} ${OPENEXR_LIBRARIES})
